import { ColorChangerPipe } from './color-changer.pipe';

describe('ColorChangerPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorChangerPipe();
    expect(pipe).toBeTruthy();
  });
});
